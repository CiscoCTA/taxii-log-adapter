taxiiService:
    pollEndpoint: https://taxii.cloudsec.sco.cisco.com/skym-taxii-ws/PollService/
    username: user
    password: pass
    feeds:
        - collection_name
    statusFile: target/taxii-status.xml

schedule:
    cron: "0 0 * * * *"

transform:
    stylesheet: config/stix2json.xsl

logging:
    config: config/logback.xml

# Uncomment the following lines to use HTTP proxy when performing TAXII requests

# proxy:
#    url: http://localhost:8002/
#    authenticationType: basic
#    username: user
#    password: pass
